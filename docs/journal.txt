upwork-agent journal
====================

Newest entry last. One entry per session: what changed, which files, test delta.


2026-09-01 — fixed-price floor, gate-detail reporting, go live
--------------------------------------------------------------
Resumed monitoring after a session disconnect. The agent had kept running
throughout: poller and auth online under pm2, working tree clean, nothing lost.

Found and closed a hole in screening. At 20:09 a $10 fixed-price GoHighLevel
posting cleared every gate, scored 76.2 — second-highest recorded — and built a
draft that would have spent 8 Connects to bid $9. Only autoApply.live being
false kept it from submitting.

Root cause: scope_fits_budget measures deliverables per $100. Being a ratio, it
can only judge scope relative to budget and cannot floor the budget itself. A
posting with no countable deliverables yields a ratio of zero and passes at any
budget at all. Hourly work already had minHourlyRate; fixed-price had nothing.

Fixed with minFixedBudget ($50), emitted under the existing rate_acceptable
gate name so "the money is too small" stays one rejection reason across both
job types. The check reads the budget alone and never the description, so no
phrasing can buy a tiny budget past it.

That naming choice created a second, smaller problem: with one gate name
covering two job types, a rising counter could not tell you which floor was
doing the work — the new floor was invisible in the status report from the
moment it shipped. Closed by carrying the newest detail string alongside each
count. First run confirmed the floor working in the wild:

    14  rate_acceptable          latest: fixed budget $10, floor $50

Flipped autoApply.live to true at 21:31 UTC on the operator's decision, with
the confirm_draft step still never having executed. Everything upstream is
proven in production; the act of submitting is not.

Also observed, not fixed: a single poller crash at 15:53 on an unguarded read
of connectsBalance from the account payload. One occurrence, self-healed via
backoff, not recurring. It will recur eventually.

Files
  src/screen/gates.ts        (+18 -0)   minFixedBudget config + fixed-price gate
  src/screen/gates.test.ts   (+22 -0)   3 tests
  config/default.json        (+3 -2)    minFixedBudget 50; autoApply.live true
  src/status.ts              (+13 -3)   newest detail per rejection reason
  docs/journal.txt           (new)      this file

Commits
  3d44469  fix(screen): floor fixed-price budgets, which the ratio gate could not
  aa95e03  fix(status): show what tripped each gate, not just how often
           feat(submit): go live — auto-apply now submits
  merged as PR #15 (76b7407), PR #16 (eec8d33); PR #17 open

Tests: 77 -> 80. Added 3, removed 0. npm run typecheck clean.
Note: this repo has no build script; typecheck is the equivalent gate.

Next: soak test 2026-09-02. Watch the first live submission — confirm_draft has
never run. Kill switch is touch /root/.upwork-agent/STOP, checked before
anything spends. Six scored jobs remain stranded; auto-apply only fires at
screening time, so they need a manual npm run apply or nothing will pick them
up.


2026-09-01 (cont.) — went live, first submission, tone feedback
---------------------------------------------------------------
Flipped autoApply.live to true at 21:31 UTC and restarted the poller. The
restart landed mid-draft on a job that had just cleared every gate — "API
Integration with Trellis Law", 69.5, zero proposals, hourly $15-35 — and killed
the apply in flight. The job was left in `scored`, which nothing sweeps, so it
was stranded within minutes of that exact failure mode being written up in
docs/database.md. Restarting without checking for an in-flight apply is the
mistake; there is no drain and nothing retries.

Recovered it by hand. The re-screen at submit refused it on freshness (14 min
old, window 10), correctly — it was only stale because of the restart. Applied
with UPWORK_AGENT_CONFIG pointed at a scratch config with a wider window, which
leaves the running poller untouched: no restart, no interval where the live
agent might bid on stale postings itself. Every other gate stayed in force.

    SUBMITTED — 2094904162340335617 · $20/hr · 20 connects · letter 3763 chars

That is confirm_draft executing for the first time. It works. Every stage of
the pipeline has now run in production, including the one that spends money.

Connects cost 20, not the 8-13 estimated from the earlier $9 draft. Upwork
prices per posting and the spread is wide. At maxPerDay 3 a full day is closer
to 60 connects than 40, so against a 147 balance and connectsReserve 30 the
agent has roughly two days of unattended running before it stops itself. Worth
deciding whether maxPerDay 3 is right before a full soak rather than after.

Operator feedback on the drafting: the letters read as too dramatic. Not fixed
this session, recorded for the next one. The register is structural rather than
incidental, so it is a prompt problem in draft/compose.ts and
draft/instructions.ts, not a wording tweak. From the submitted letter:

  - ALL-CAPS section headers throughout: "THE PART THAT BREAKS ISN'T THE API
    CALLS", "WORK THAT LOOKS LIKE YOURS", "WHAT I HAVEN'T DONE".
  - Opens on a contrarian negation and a stakes-raising hook rather than on the
    work: "The thing that will actually cost you money is a new case appearing
    at 2am and nothing happening."
  - Vivid worst-case imagery: "you eventually SMS the wrong person about a
    lawsuit".
  - Aphoristic closers: "silent failure is worse than no delivery", "the
    version you can trust on a Monday morning".
  - Escalating fragments and arrow bullets used for rhythm.

The underlying content is sound — specific, honest about gaps, ends on a real
question. It is the delivery that overreaches. Whoever picks this up should
change the tone instructions and compare against this letter as the before.

Also still open from earlier in the session:
  - Six jobs stranded in `scored`; nothing sweeps that state.
  - The connectsBalance crash at 15:53 remains unguarded.
  - README.md still says scoring, drafting and submission are "not built" and
    quotes 19 tests. All three are built and there are 80. Stale, not touched.

Files
  config/default.json    autoApply.live true
  CLAUDE.md              (new)  repo had none
  docs/architecture.md   (new)
  docs/database.md       (new)
  docs/journal.txt       this entry

Tests: 80, unchanged. npm run typecheck clean.


2026-09-02 — soak check, then Trust & Safety restriction; automation stopped
-----------------------------------------------------------------------------
Read-only soak of the first 20 hours unattended. Ended with the account
restricted and both processes shut down. No code changed this session.

The run itself was stable: 393 poll cycles at 20/hour from 09-01T14:00, zero
restarts, one error (the 09-01 connectsBalance crash, self-recovered). 196 jobs
seen, 72 screened, 3 submitted and confirmed live on Upwork. 36 connects spent,
balance 130.

Findings, in the order they matter:

  1. The daily cap is first-come, not best-of. Three proposals landed inside 80
     minutes (21:44, 22:56, 23:02 on 09-01) scoring 69.5, 65.4 and 72.8. In the
     22 hours after, six jobs cleared every gate and were refused with "3
     proposals in 24h" — scoring 76.0, 76.4, 81.8, 85.0, 91.4, 91.8. Every
     refused job outranked every submitted one. They sit in `scored`, which
     nothing sweeps; 12 jobs are stranded there.

  2. The drafter names a disqualifier and submits anyway. Both auto-submissions
     went to the same client (8658048), two near-duplicate $50 postings six
     minutes apart, 16 connects. The letters say "I don't operate a marketing or
     lead gen agency" in their own WHERE I DON'T MATCH block. Nothing gates on
     that, and there is no per-client dedupe.

  3. isAuthError misses the real message. poll.ts:47 matches "token expired" and
     "token has expired" but not "token is expired", which is what Upwork sends.
     When authorization was revoked at 10:47 the guard did not fire, no alert
     was raised, and the poller treated a permanent failure as transient and
     backed off. The alerts table has never had a row; the email channel is
     still unproven in production.

  4. Search coverage, traced from one missed posting. A Forward Deployed
     Engineer role (client $249k spend, 4.99/21, $20-40/hr, TypeScript/Claude/
     MCP) never reached the database. Cause is query shape, not gates: adding
     one word the posting lacks evicts it. "Forward Deployed Engineer" ranks it
     first; + "automation" or + "webhook" loses it entirely. The word
     "automation" appears nowhere in that posting, and neither does "webhook" —
     so api-integration and ai-agents, the two profiles built to catch it, each
     miss on their own third word. find_jobs action=smart_search returns it at
     rank 3. The dashboard's matching_jobs feed is a third surface we never read.

  5. Detection latency is bimodal, not a flat 34 min median. Split by whether
     Upwork published a proposal count: omitted (fresh postings) n=104, median
     3.9 min; reported (aged postings) n=96, median 2135 min. The 3-minute loop
     is fast when the surface cooperates. Note our own pool data is censored
     twice — proposals_max 15 hides the tail, and 69 of the recorded zeros are
     inferred rather than observed.

At 10:47:44 UTC the poller failed with "Refresh token is expired or revoked or
not registered". That was not an expiry. Upwork Trust & Safety restricted the
account for automated activity; their ticket is timestamped 12:47 CEST, the same
minute. Detection was, by their own notice, fully automated with no human review.

Stopped at 10:50 UTC: touch ~/.upwork-agent/STOP, pm2 stop on both processes,
pm2 save so a reboot cannot resurrect them. Nothing on this box has contacted
Upwork since 10:47.

An appeal was filed. Its position: access ran through Upwork's own official MCP
server under OAuth Upwork issued, so the "without permission" qualifier in their
notice does not apply; none of their enumerated categories (scraper, browser
extension, refresh tool, third-party software) describe it; three proposals, all
individually written, connects spent on each, no client messaged and no other
user affected.

Still open, unchanged:
  - README.md says scoring, drafting and submission are "not built" and quotes
    19 tests. All three are built and there are 80.
  - Letter tone is still the register flagged on 09-01: ALL-CAPS headers,
    contrarian cold open, arrow bullets. Both auto-sent letters have it.
  - connectsBalance deref unguarded at poll.ts:111 and apply.ts:52.

Files
  (none in the repo — read-only session)
  ~/.upwork-agent/STOP   created
  /root/.pm2/dump.pm2    saved with both processes stopped

Tests: 80, unchanged. npm run typecheck clean.


2026-09-04 — letter register: rewrote the compose prompt around genre
---------------------------------------------------------------------
Closes the drafting feedback left open on 09-01 and still listed as unfixed on
09-02. No pipeline ran and nothing contacted Upwork; the account remains stood
down.

The 09-01 note recorded five tics (ALL-CAPS headers, a contrarian stakes-raising
opener, vivid worst-case imagery, aphoristic closers, arrow bullets used for
rhythm) and called the register structural rather than incidental. Regenerating
a letter for a live posting reproduced all five, which confirmed that reading.

The sharper diagnosis is genre, not tone. The old SYSTEM opened "THIS IS A SALES
PITCH", and the model read that as a brief to produce marketing: sectioned,
quotable, a metrics block, an explainer addressed to an audience, a paragraph of
positioning for an adjacent product. That is a capabilities page, not a message
to one buyer. The tics follow from the genre, which is why treating them as
wording would not have held.

Rewrote SYSTEM around what is being written:
  - "A message to one person. Not marketing, not a capabilities page, not a
    LinkedIn post", with an explicit test — if it would work unchanged as a
    landing page it is wrong, and a proposal is one that could only have been
    sent to this client about this post.
  - ANSWER THEIR POST: use their words, engage what they actually wrote.
  - MAKE ONE ARGUMENT: one case, evidence in support of it rather than on
    display, spoken figures rounded rather than a spec-sheet row.
  - Gaps go in a sentence in the flow, not a section of their own.
  - Explicit avoid-list for the five tics, plus a hard rule against em and en
    dashes in any position.
  - Target length 3000-4000 chars -> 1200-1500, on the reasoning that length is
    the clearest genre tell.

Kept verbatim: the evidence-grounding rule, the "Do not claim" handling, the
compliance-marker and injection rules, and the ban on writing that he is the
wrong hire.

Measured on the same posting, same evidence, same model: 4207 chars -> 1681, no
headers, no dashes, opens by quoting the client's own brief, closes on two
questions specific to their setup.

Two things this session did not fix:
  - verify.ts still checks only length, compliance markers and agent-directed
    text. It does not check claims against evidence, and it does not check the
    dash ban. Both are prompt-only, and a prompt instruction is a hope rather
    than a control -- the same lesson the length retry already encodes.
  - The drafting path still cannot be exercised without MCP auth. draft-one.ts
    takes a job id and fetches the posting, so a pasted posting needed a local
    runner in the gitignored data/ to reuse the pipeline.

Files
  src/draft/compose.ts   SYSTEM rewritten
  docs/journal.txt       this entry

Tests: 80, unchanged. npm run typecheck clean.


2026-09-04 (cont.) — the rubric could not see the product half of the profile
------------------------------------------------------------------------------
Re-authorized the Upwork MCP server, on Mahi's explicit decision after the risk
was put to him. This is the Claude Code MCP client only, for reads. The agent's
own token store is still the dead one from 09-01, both pm2 processes are still
stopped, STOP still exists and autoApply is still false. Five read calls this
session (list_accounts, four find_jobs action=get). Nothing was spent.

Ran four live postings through screen() with fresh and pool_small neutralised,
since action=get supplies neither: getJob hardcodes skillTags [] and
proposalCount null, and only the search hit carries them. All four rejected.
Three of them, all product/growth/strategy roles, failed on `relevant` alone.

The cause is that screen.skills was 60 entries of tools and technologies with no
product vocabulary at all. Mahi's Upwork profile already carries Product
Management, AI Product Management, SaaS Development, Minimum Viable Product and
Systems Modeling as skill tags; the rubric mirrored none of them. His CV carries
eight years of product leadership -- Porter 200K to 2M monthly actives and a
subscription model adding $12M ARR, Adevinta search ranking +10% CTR, Printify
activation +5pp -- and a corporate strategy role before that. The screener could
not see any of it, so it was rejecting jobs he is arguably overqualified for.

Corpus: added product-growth-and-positioning (flagship). The load-bearing item
is pip0's failed go-to-market -- launched standalone, did not break through,
stopped within weeks of the evidence and repositioned as the engine inside
WhatsScale. Both drafts generated afterwards led with it, which is the right
call: for positioning work the launch he got wrong is stronger evidence than the
ones that worked. Also carries WhatsScale's organic acquisition (1,400 signups
in eight months, ~500/month, no paid spend, no sales team) and Jindal's market
entry work. _reindex.sh run, 21 chunks. The corpus is outside the repo, so none
of this is in this commit.

Deliberately NOT added: +18pp week-1 activation. _verification.md excludes it on
purpose -- only cite it if the sample size and measurement window can be
defended from memory. Flagged to Mahi rather than overridden.

Config: screen.skills and score.skills 60 -> 80, adding product management,
product manager, product strategy, strategist, growth, positioning,
go-to-market, gtm, activation, adoption, product-led, roadmap, retention, churn,
monetisation, monetization, marketplace, mvp, minimum viable product.

Re-assessed after the change:
  APPLY   Business Strategist for SaaS Growth   all gates passed
  REJECT  B2B SaaS Growth Marketer              client_pays, client_leaves_feedback
  REJECT  Product & Growth Strategist           client_pays, client_leaves_feedback
  REJECT  Call and WhatsApp Automation Expert   client_pays, client_leaves_feedback, rate_acceptable

The Business Strategist posting has the best client of the four by a wide margin
($50,039 spend, 61 contracts, 26 reviews, 4.74) and was being rejected on one
gate, on vocabulary. That is the concrete cost of the blind spot.

Known cost of the widening: `growth` and `strategist` are broad, so `relevant`
will now admit growth-marketing and sales-adjacent postings -- the category the
gate was added for after a sales role cleared everything and scored 76.8.
`relevant` is a floor rather than a ranking, so scoring still sorts them, but
this is a real loosening and should be watched before any unattended run.

Also observed: the account is now on Freelancer Basic, not Freelancer Plus, so
bid_stats_available is false. Mahi confirms the subscription lapsed and was not
renewed. Not related to the restriction. RESUME.md still says Plus and is stale
on this point.

Files
  config/default.json    screen.skills and score.skills, 60 -> 80
  docs/journal.txt       this entry
  (outside the repo)     ~/upwork-profile/corpus/product-growth-and-positioning.md, TAGS.md

Tests: 80, unchanged. npm run typecheck clean.


2026-09-04 (cont.) — first submissions since the restriction, three by hand
----------------------------------------------------------------------------
Three proposals submitted through the MCP server, each one previewed, presented
and confirmed individually by Mahi. No code ran to produce them beyond the
drafting library; poll.ts never started.

  SUBMITTED  2095830415708221441  AI Enterprise Architecture Consultant
             $200 fixed ·  7 connects + 1 boost bid · letter 1853 chars
             5 screening answers · portfolio: pip0, WhatsScale
  SUBMITTED  2095830813961609217  Business Strategist for SaaS Growth
             $30/hr    · 17 connects · no boost
  SUBMITTED  2095834325717839873  Product & Growth Strategist for SaaS
             $35/hr    ·  8 connects · no boost

Balance 130 -> 98, verified against connects_balance rather than arithmetic.
The boost bid is not in the ledger: it is charged only on a top-4 finish or
client engagement, so 1 connect is still outstanding on the first.

Notes from doing this by hand that the automated path does not know:

  1. The kill switch does not cover the MCP path. STOP is read at
     submit/run.ts:57, which is the agent's own submit. A direct
     manage_proposals call bypasses it entirely. Nothing in this repo could
     have stopped these three.

  2. Boost is a create-time parameter, not a confirm-time one, so adding a
     boost means re-issuing the preview. Only one preview is held per action
     type and a second create silently supersedes the first, which is why
     these had to be done strictly one at a time.

  3. Boost state varies per job and is worth reading. Two of the three had an
     empty auction where 1 connect ranks first. The Business Strategist posting
     already had bids of [5 4 3 2]; we skipped it there, on the grounds that
     17 connects on an open-ended hourly role with a client who hires BDRs is
     already the weakest spend of the three.

  4. getJob supplies neither skillTags nor proposalCount — both come from the
     search hit — so any gate assessment from action=get alone cannot evaluate
     `relevant` on tags or `pool_small` at all. Worth remembering before
     trusting a get-only screen.

  5. scope_fits_budget has a false positive worth fixing. The AI architecture
     posting is a 30-minute call for $200 and scored 22.5 deliverables, 11.25
     per $100, against a 1.5 floor. countDeliverables counts prose bullets, so
     a long well-written brief describing what the CLIENT is building reads as
     scope the freelancer must deliver. It also took +2 twice from HEAVY_SCOPE
     for "not starting from scratch" and "not a request to build the full
     system" — both phrases where the client is saying the opposite. Under
     autoApply this would have rejected the best-fit posting of the five.

Screening answers ran through draft/answers.ts for the first time in
production. All five were grounded; spot-checked read-only server-side and the
allow-rule override against mcp-agent-tooling:14,19, the three rewrites against
claude-code-scaffolding:15, and the 10-dimension stress test against :29,33.

Files
  docs/journal.txt   this entry
  (outside the repo) data/ runner scripts, gitignored

Tests: 80, unchanged. npm run typecheck clean.


2026-09-04 (cont.) — three more submissions, and four screening gaps found
----------------------------------------------------------------------------
Assessed nine live postings against the gates and submitted three more, taking
the day to six proposals and 68 connects. Balance 130 -> 74, with a +12
"Behavioral based subsidy" from Upwork at 12:04 unrelated to anything we did.

  SUBMITTED  2095856769312976897  Principal AI-Native GTM Engineer 0->$5M ARR
             $125/hr · 18 connects · 5 screening answers
  SUBMITTED  2095856845305053185  AI Training - specific (Cork)
             $65/hr  ·  8 connects
  SUBMITTED  2095856914168979457  AI Automation for Business Tasks
             $45/hr  · 10 connects

The GTM posting passes `relevant` only because of this morning's config change:
it matches on `gtm`, a term added hours earlier. Before that it was invisible,
and it is the highest-rate posting seen to date ($100-200/hr, MANUAL).

Operator correction, applied to the corpus rather than to one letter:
availability-and-positioning said "typically 15-30 hrs/week". Mahi states he
can work full time and well beyond. The chunk now says 35-40 is comfortable,
with an explicit instruction not to hedge it down. This changes every future
draft, not just this one. Before the fix the GTM letter offered 30 hrs/week
against a stated 35-40 requirement, and screening answer 1 opened by
disclaiming the $5M bar rather than leading with Porter's $12M.

Four screening gaps found, in order of what they cost:

  1. LOCATION. A Singapore posting passed every gate and was unwinnable: "Be
     based in Singapore", plus "[IMPORTANT] Existing Singapore client case
     studies". 18 connects. No gate models location. Note the fix is NOT a
     prose regex as first assumed — `find_jobs action=get` returns a structured
     `preferred_locations: {countries, location_required}` block, which
     upwork.ts does not read at all. But the Singapore posting carried no such
     block, so prose is still needed as a fallback. Field first, regex second.

  2. FIXED-PRICE EFFORT. A market research posting at $320 fixed for 6 months
     at 2 hrs/week is about $6/hr. `rate_acceptable` passes because
     minFixedBudget is absolute and cannot see duration; scope_fits_budget
     passes because the posting is four lines with no bullets, so
     countDeliverables returns ~0. Both money gates wave it through. Hourly
     postings carry engagementType; fixed-price has no effort denominator.

  3. SCOPE FALSE POSITIVE (recorded earlier today, now with a second instance).

  4. FULL-TIME CAPACITY. `engagementType: FULL_TIME` is in the payload and
     nothing reads it. Not currently a rejection since Mahi is available full
     time, but the field is there and unused.

Boost auction data, worth recording because it varies far more than expected
and is only visible in the create preview:
  AI Enterprise Architecture   []              1 connect ranks 1st
  Product & Growth Strategist  []              1 connect ranks 1st
  Business Strategist          [5 4 3 2]       skipped
  AI Training (Cork)           [30 3 2 1]      skipped
  AI Automation                [25 17 5 1]     skipped
  GTM Engineer                 [30 10 6 5]     skipped
A single 25-30 connect bid appears on three of them, which on an 8-18 connect
posting is someone spending two to three times the application cost on rank.

Files
  docs/journal.txt   this entry
  (outside the repo) corpus availability-and-positioning.md updated

Tests: 80, unchanged. npm run typecheck clean.
